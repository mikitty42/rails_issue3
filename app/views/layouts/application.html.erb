<!DOCTYPE html>
<html>
  <head>
    <title>RailsIssue3</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% flash.each do |key,value| %>
    <%= content_tag(:div,value,class: "#{key}") %>
  <% end %>
   <% if logged_in? %>
    <header>
    <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Profile',user_path(current_user.id) %></button>
    <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Logout',session_path(current_user.id),method: :delete %></button>
  <% else %>
        <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Login',new_session_path %></button>
        <button type="button" class="btn btn-default navbar-btn"><%= link_to 'Sign up',new_user_path %></button>
    </header>
  <% end %>
    <%= yield %>
    <footer>
      <p>運営会社 aaa株式会社</p>
      <p>© 2015-2016 aaa Corp.</p>
    </footer>
  </body>
</html>
